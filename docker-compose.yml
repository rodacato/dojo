services:
  dojo-api:
    build:
      context: .
      dockerfile: apps/api/Dockerfile
    restart: unless-stopped
    env_file: .env
    depends_on:
      db:
        condition: service_healthy
    ports:
      - "3001:3001"
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:3001/health"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 15s

  web:
    build:
      context: .
      dockerfile: apps/web/Dockerfile
    restart: unless-stopped
    depends_on:
      - dojo-api
    ports:
      - "80:80"
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost/health"]
      interval: 30s
      timeout: 5s
      retries: 3

  piston:
    image: ghcr.io/engineer-man/piston
    restart: unless-stopped
    tmpfs:
      - /piston/jobs:exec,size=256m
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:2000/api/v2/runtimes"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 10s
    # No ports exposed — only api service can reach piston via internal network

  db:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-dojo}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB:-dojo}
    volumes:
      - postgres-data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-dojo}"]
      interval: 10s
      timeout: 5s
      retries: 5
    # No ports exposed — only api service can reach db via internal network

volumes:
  postgres-data:

export function PageLoader() {
  return (
    <div className="flex items-center justify-center h-screen bg-base">
      <span className="font-mono text-muted animate-pulse">
        loading<span className="text-accent">_</span>
      </span>
    </div>
  )
}
